# attend — 생각공작소 근태 PWA

행정직 출퇴근·연차 앱. 백엔드 = 메인 레포 `scripts/19_근태관리/`(GAS standalone).

- 원래 `saenggak/attend/`였으나 **허브 PWA scope(`/saenggak/`)가 별개 앱 설치를 억제**해 별도 레포로 이사 (2026-07-21, pdfpng 선례와 동일 원인·처방).
- 배포 = GitHub Pages(main·`/`) → https://think-fact0ry.github.io/attend/ — push가 곧 라이브.
- `sw.js`의 `CACHE`는 배포마다 버전을 올릴 것.
- 아이콘은 이 레포가 자체 보유(`/attend/…` 경로) — saenggak 공유 자산 참조 금지(scope 독립).
